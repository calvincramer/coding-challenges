class Solution:
    def countVisitedNodes(self, edges: list[int]) -> list[int]:
        # Find strongly connected components
        # answer is size of the strongly connected component plus the answers of all it's children SCCs
        pass
