"""
For each pair of distinct edges:
    Calculate XOR score of three connected components (using explore)
    Keep min score


"""


class Solution:
    def minimumScore(self, nums: list[int], edges: list[list[int]]) -> int:
        pass
