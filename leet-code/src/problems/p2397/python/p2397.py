from typing import List


class Solution:
    def maximumRows(self, matrix: List[List[int]], numSelect: int) -> int:
        # No:
        # colSum = [sum(row) for row in matrix]
        # colSum.sort()

        return None
