class Solution:
    def isTransformable(self, s: str, t: str) -> bool:
        # Just determine lexicographic order
        # since

        # starting right to left determine if can bubble up char from s to correct location in t
        pass
